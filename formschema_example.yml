segmentation:
  - name: Segmentation Algorithm,
    description: Choose which segmentation algorithm to use
    key: segmentation
    formElement: radio
    type: string
    options:
      - label: Cellpose
        value: cellpose

  - name: Cell Probability Threshold,
    description: Cell probability threshold parameter for Cellpose
    key: cell_prob_threshold
    formElement: slider
    type: float
    min: -6
    max: 6
    step: 0.1

  # ... further options within the segmentation category

dot-detection:
  # Fields in the dot-detection category

# Other categories
